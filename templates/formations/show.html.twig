{% extends 'base.html.twig' %}

{% block title %}Formations{% endblock %}

{% block body %}
    <div class="container">
    <h1>Formations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary"  href="{{ path('formations_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('formations_edit', {'id': formation.id}) }}">edit</a>
  {% if  %}
    {{ include('formations/_delete_form.html.twig') }}
{% endblock %}
