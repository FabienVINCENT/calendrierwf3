{% extends 'base.html.twig' %}

{% block title %}Edit Endroit{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit Endroit</h1>

    {{ include('endroit/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="row">
    <a href="{{ path('endroit_index') }}">Retour menu</a>

    {{ include('endroit/_delete_form.html.twig') }}
    </div>
  </div>  
{% endblock %}
