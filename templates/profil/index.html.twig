{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<div class="mt-3 ml-3">
    <h1>Fiche Utilisateur</h1>
    <div>Nom & Prénom: {{formateurs.lastname}} {{formateurs.firstname}}</div>
    <div>Mail: {{ formateurs.email }}</div>
    <div>Téléphone: {{ formateurs.phoneNumber }}</div>
    <div>Compétences: </div>
    <div>Formations en cours: </div>
    {# <a href="{{ path('profil_edit', {'id' : formateurs.id}) }}">Modifier</a> #}
</div>
{% endblock %}
